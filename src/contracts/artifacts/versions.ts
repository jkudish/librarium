export const ARTIFACTS_VERSION = '1.0.0' as const;

export const ARTIFACT_VERSIONS = {
  run_manifest: ARTIFACTS_VERSION,
  provider_metadata: ARTIFACTS_VERSION,
  sources: ARTIFACTS_VERSION,
  jsonl_record: ARTIFACTS_VERSION,
} as const;
